{
  "name": "yt-proxy",
  "version": "2.0.0",
  "description": "YouTube proxy server — scrapes YouTube directly, no API key needed",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}